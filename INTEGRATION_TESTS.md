# 結合テスト項目書

`code-chat` を、実際の Gemini API・MCP サーバー・ChromaDB・OAuth に接続して確認する、手動の結合テストの項目書です。

## 目次

- [1. 目的と範囲](#1-目的と範囲)
- [2. 事前準備](#2-事前準備)
- [3. 記録の方法](#3-記録の方法)
- [4. テスト項目](#4-テスト項目)
  - [A. 認証 (API キー / OAuth)](#a-認証-api-キー--oauth)
  - [B. 基本の対話](#b-基本の対話)
  - [C. Write モード (`-w`)](#c-write-モード--w)
  - [D. レビューとコミットメッセージ](#d-レビューとコミットメッセージ)
  - [E. モデル一覧](#e-モデル一覧)
  - [F. RAG](#f-rag)
  - [G. MCP](#g-mcp)
  - [H. Context Caching](#h-context-caching)
  - [I. エラーとリトライ](#i-エラーとリトライ)
  - [J. 引数の組み合わせ](#j-引数の組み合わせ)
- [5. 実施記録](#5-実施記録)

---

## 1. 目的と範囲

`pytest` の単体テスト (`tests/`) は、外部 API や MCP サーバーへ接続せず、モックで検証します (カバレッジ 100%)。この項目書は、モックでは確認できない、次の点を確認します。

- 実際の Gemini API・MCP サーバー・ChromaDB・OAuth と連携して、動作すること。
- Gemini API の実際のエラー (429 / 503 / 400 / 404) に対して、リトライとエラー表示が、想定どおりであること。
- サブコマンドやオプションの組み合わせが、実際の環境で、想定どおりに動くこと。

対象外: 内部関数の単体の振る舞い (単体テストで確認済み)、性能・負荷の測定。

## 2. 事前準備

### 2.1 認証情報

| 種別 | 環境変数など | 用途 |
| --- | --- | --- |
| 無料枠の API キー | `GEMINI_API_KEY` に、無料枠のプロジェクトのキーを設定 | 通常の項目。使用回数の制限は、ありません (上限は 1 分あたり 5 リクエスト程度) |
| 有料枠の API キー | `GEMINI_API_KEY` に、課金を有効にしたプロジェクトのキーを設定 | 「H. Context Caching」のみ。**料金が発生します** |
| OAuth | `GEMINI_OAUTH_CLIENT_ID` / `GEMINI_OAUTH_CLIENT_SECRET` を設定し、`code-chat --login` を実行済み | 「A. 認証」 (手順は [OAUTH.md](OAUTH.md)) |

- 各項目の「手順」に書いた API キーの種別を使ってください。
- 有料枠のキーを使う項目 (H) は、費用の発生を了承した上で、実施してください。作成したキャッシュは、実施後に `code-chat cache rm` で削除します。

### 2.2 環境

- `uv sync` 済みで、`uv run code-chat` (または、インストール済みの `code-chat`) が実行できること。以降の手順では、`code-chat` と表記します。
- MCP の項目 (G): `~/.config/code-chat/mcp.json` に、有効な MCP サーバーが 1 つ以上あること (例: `filesystem`)。`npx` / `uvx` が使えること。
- Git の項目 (D): Git リポジトリの中で実行し、変更 (差分) を用意すること。
- テスト用のディレクトリを用意すること。Write モード (C) は、テスト用のファイルにだけ実行し、リポジトリのソースは、対象にしないでください。

  ```bash
  mkdir -p /tmp/it && cd /tmp/it
  printf 'def add(a, b):\n    return a + b\n' > sample.py
  ```

### 2.3 実行時の注意

- 非対話で実行するコマンドは、標準入力を閉じてください (`< /dev/null`)。標準入力が端末でない場合、`code-chat` は、入力の待ち受けで止まることがあります。
- 無料枠は、1 分あたりの上限が小さい (`gemini-3.5-flash` で 5 リクエスト程度) ため、連続して実行すると、429 になります。429 が出ても、自動でリトライされます。項目の間は、1 分ほど空けると安定します。
- 各項目の終了コードは、`echo $?` で確認します。

## 3. 記録の方法

各項目の「結果」の列に、次のいずれかを記入します。

| 記号 | 意味 |
| --- | --- |
| OK | 期待結果のとおり |
| NG | 期待結果と異なる (「5. 実施記録」に、内容を書く) |
| - | 未実施 / 対象外 (理由を「5. 実施記録」に書く) |

## 4. テスト項目

表の列は、次のとおりです。「手順」のコマンドは、`< /dev/null` を省略しています (2.3 を参照)。

### A. 認証 (API キー / OAuth)

| ID | 確認項目 | 手順 | 期待結果 | 結果 |
| --- | --- | --- | --- | --- |
| IT-A-01 | API キーで応答が返る | 無料枠のキーで `code-chat "1+1は? 数字だけ答えて"` | 応答 (`2`) が表示される。終了コード 0 | |
| IT-A-02 | API キーが未設定 | `GEMINI_API_KEY` を解除して `code-chat "hi"` | 「環境変数 GEMINI_API_KEY が設定されていません」と、`--oauth` の案内が表示される。終了コード 1。OAuth には、自動で切り替わらない | |
| IT-A-03 | API キーが無効 | `GEMINI_API_KEY=invalid code-chat "hi"` | `[HTTP 400 ...] API キーが無効です` と、`GEMINI_API_KEY` を確認するヒントが、1 回だけ表示される。終了コード 1 | |
| IT-A-04 | OAuth ログイン | `code-chat --login` | ブラウザで認証でき、`~/.config/code-chat/oauth_token.json` が作成される (権限 `-rw-------`)。終了コード 0 | |
| IT-A-05 | OAuth で応答が返る | `code-chat --oauth "1+1は? 数字だけ答えて"` | 応答が表示される。`GEMINI_API_KEY` が設定されていても、OAuth が使われる | |
| IT-A-06 | OAuth の設定がない | `GEMINI_OAUTH_CLIENT_ID` を解除し、トークンを退避して `code-chat --login` | 設定手順のメッセージが表示される。終了コード 1 | |
| IT-A-07 | 未ログインの非対話実行 | トークンを退避して `code-chat --oauth "hi"` | `code-chat --login` の実行を促すエラーで終了する (ブラウザは開かない)。終了コード 1 | |
| IT-A-08 | OAuth の無料枠 | `--oauth` と無料枠の API キーで、それぞれ、同時に多数のリクエストを送る (I-01 の手順) | どちらも、429 のエラーに `...-FreeTier` を含み、上限が同じ (`gemini-3.5-flash` は 1 分あたり 5 リクエスト) | |

### B. 基本の対話

| ID | 確認項目 | 手順 | 期待結果 | 結果 |
| --- | --- | --- | --- | --- |
| IT-B-01 | ワンショット | `code-chat "Pythonでリストを逆順にする方法は？"` | 応答が、ストリーミングで表示され、終了する。終了コード 0 | |
| IT-B-02 | ファイルをコンテキストにする | `code-chat "この関数を説明して" -f /tmp/it/sample.py` | ファイルの内容 (`add`) に基づく説明が表示される | |
| IT-B-03 | ディレクトリをコンテキストにする | `code-chat "ファイル構成を説明して" -f ./packages/code-chat-cli/src` | 複数ファイルを読み込み、説明が表示される。読み込んだ件数のログが出る | |
| IT-B-04 | 存在しないパス | `code-chat "hi" -f /no/such/path` | 「パス '...' が見つかりません」で終了する。終了コード 1 | |
| IT-B-05 | パイプ入力 | `echo "print(1)" \| code-chat "このコードを説明して"` | 標準入力の内容が、コンテキストとして使われる | |
| IT-B-06 | 対話モード | 引数なしで `code-chat` を実行し、質問を 2 回入力して、`exit` | 各回で応答が返り、`exit` で終了する。終了コード 0 | |
| IT-B-07 | 会話の文脈 | 対話モードで、「私の名前は太郎です」→「私の名前は？」 | 2 回目で、「太郎」と答える (会話の履歴が保たれる) | |
| IT-B-08 | `/save` | 対話モードで、質問のあと、`/save /tmp/it/log.md` | ログが Markdown で保存され、保存先が表示される | |
| IT-B-09 | `/save` のパス省略 | 対話モードで `/save` | 「保存先のファイルパスを指定してください」と表示され、対話が続く | |
| IT-B-10 | 自動保存 (`-a`) | `code-chat -a "hi"` (`/tmp/it` で実行) | 終了時に `<日時>_chat.md` が作成される | |
| IT-B-11 | モデルの指定 (`-m`) | `code-chat -m gemini-3.5-flash "hi"` | 指定したモデルで応答する | |
| IT-B-12 | ドライラン | `code-chat "テスト" -f /tmp/it/sample.py --dry-run` | API を呼ばず、解析結果 (対象パス、コンテキスト長など) が表示される。終了コード 0 | |
| IT-B-13 | デバッグログ | `code-chat -D "hi"` | DEBUG のログが出力される。Gemini API のエラーの詳細は、エラー時のみ追加で出る | |
| IT-B-14 | ライブラリの通信ログ | `code-chat --trace "hi"` | `httpx` など、ライブラリの通信ログが出力される | |

### C. Write モード (`-w`)

対象は、`/tmp/it/sample.py` のみにしてください。

| ID | 確認項目 | 手順 | 期待結果 | 結果 |
| --- | --- | --- | --- | --- |
| IT-C-01 | 承認して上書き | `code-chat "型ヒントを追加して" -f /tmp/it/sample.py -w` → `y` | 変更内容の一覧 (ファイル名、行数) と確認が表示され、`y` でファイルが更新される。`sample.py.bak.orig` と、タイムスタンプ付きのバックアップが作成される | |
| IT-C-02 | 拒否 | 同じ手順で `N` (または、空入力) | 「上書きをキャンセルしました」と表示され、ファイルは変わらない | |
| IT-C-03 | バックアップの世代数 | C-01 を 7 回以上繰り返す | タイムスタンプ付きのバックアップは、直近 5 世代だけ残る。`.bak.orig` は残る | |
| IT-C-04 | 省略コードの警告 | 「関数 add は変更せず、`# ...` だけを書いて」のように指示して実行 | 「[省略の可能性あり]」の警告が表示される | |
| IT-C-05 | `-f` なしの `-w` | `code-chat "hi" -w` | 「対象のファイルパスが指定されていません」のエラーが記録される | |
| IT-C-06 | 対話モードでの `-w` | `code-chat -f /tmp/it/sample.py -w` で、指示を入力 | 応答のあと、C-01 と同様に、確認が表示される | |

### D. レビューとコミットメッセージ

| ID | 確認項目 | 手順 | 期待結果 | 結果 |
| --- | --- | --- | --- | --- |
| IT-D-01 | ファイルのレビュー | `code-chat --review -f /tmp/it/sample.py` | レビュー結果が、ストリーミングで表示される。終了コード 0 | |
| IT-D-02 | Git 差分のレビュー | 作業ツリーに変更がある状態で `code-chat --review` | 差分のレビューが表示される | |
| IT-D-03 | ステージ済み差分のレビュー | 変更を `git add` して `code-chat --review --staged` | ステージ済みの差分が対象になる | |
| IT-D-04 | 差分なしのレビュー | 変更がない状態で `code-chat --review` | 「レビュー対象のコードまたは変更点が見つかりませんでした」と表示される | |
| IT-D-05 | コミットメッセージの生成 | 変更がある状態で `code-chat -g` | Conventional Commits 形式のメッセージが表示される。終了コード 0 | |
| IT-D-06 | 差分なしのコミットメッセージ | 変更がない状態で `code-chat -g` | 「変更（git diff）が検出されませんでした」と表示される | |
| IT-D-07 | Git リポジトリの外 | Git 管理外のディレクトリで `code-chat -g` | 「git diff の実行に失敗しました」のエラーが記録される。終了コード 1 | |

### E. モデル一覧

| ID | 確認項目 | 手順 | 期待結果 | 結果 |
| --- | --- | --- | --- | --- |
| IT-E-01 | モデル一覧 | `code-chat --list-models` | 利用できるモデルの一覧が表示される。終了コード 0 | |
| IT-E-02 | 一覧で OAuth | `code-chat --oauth --list-models` | 一覧が表示される | |

### F. RAG

`GEMINI_API_KEY` が必要です (`--oauth` 併用時も、RAG の検索は API キーを使います)。Vector DB は、カレントディレクトリの `./.chroma_db` に作られます (`rag status` / `rag rm` と、`--rag` の検索は、この場所を使います)。リポジトリを汚さないよう、`/tmp/it` で実行し、`<REPO>` は、このリポジトリの絶対パスに読み替えてください。

| ID | 確認項目 | 手順 | 期待結果 | 結果 |
| --- | --- | --- | --- | --- |
| IT-F-01 | インデックスの作成 | `cd /tmp/it && code-chat rag create --input_dirs <REPO>/packages` | 「インデックス作成完了: N チャンク追加」と表示される。終了コード 0 | |
| IT-F-02 | ステータス | `code-chat rag status` (`/tmp/it` で実行) | データベースのパスと、チャンク数が表示される | |
| IT-F-03 | RAG で質問 | `code-chat "リトライの実装箇所を説明して" --rag` | 検索したコンテキストに基づく回答が、ストリーミングで表示される | |
| IT-F-04 | チャットへの RAG | `code-chat "リトライの実装箇所は？" --rag` (ワンショット) と、対話モードの `--rag` | どちらも、検索結果が付加されて、回答に反映される | |
| IT-F-05 | 差分更新 | ファイルを追加して `code-chat rag update --input_dirs <REPO>/packages` | 既存の内容を消さずに、追加される (チャンク数が増える) | |
| IT-F-06 | インデックスの削除 | `code-chat rag rm` | 内容が削除される。`rag status` でチャンク数が 0 になる | |
| IT-F-07 | 存在しないディレクトリ | `code-chat rag create --input_dirs /no/such/dir` | 「RAG (create) コマンドの実行中にエラーが発生しました」と、「指定されたインデックス対象ディレクトリが存在しません」が、トレースバックなしで記録される (`-D` または `--trace` を付けると、トレースバックも出る)。終了コード 1 | |
| IT-F-08 | インデックスがない状態での検索 | F-06 のあとに `code-chat "hi" --rag` | 検索結果が空である旨の警告が出て、RAG なしで回答する | |
| IT-F-09 | API キー未設定で RAG | `GEMINI_API_KEY` を解除し `--oauth --rag` | 「RAG には API キーが必要です」のエラーで終了する。終了コード 1 | |
| IT-F-10 | 大量のインデックス作成 (リトライ) | 多数のファイルを対象に `rag create` (無料枠) | 429 が出ても、バッチ単位でリトライされ、最後まで完了する (警告が出ることがある) | |

### G. MCP

`~/.config/code-chat/mcp.json` に、有効なサーバーが必要です。

| ID | 確認項目 | 手順 | 期待結果 | 結果 |
| --- | --- | --- | --- | --- |
| IT-G-01 | ステータス | `code-chat mcp status` | 有効なサーバーごとに、ツール一覧が表示される | |
| IT-G-02 | 導通テスト | `code-chat mcp test` | 各サーバーが `[OK] Successfully fetched N tools.` になり、`テスト完了: N/N` と表示される | |
| IT-G-03 | 無効なサーバー | `enabled: false` のサーバーがある状態で `code-chat mcp status` と `mcp test` | 無効なサーバーは、一覧・テストの対象に含まれない | |
| IT-G-04 | 起動できないサーバー | `mcp.json` に、存在しないコマンドのサーバーを追加して `code-chat mcp test` | そのサーバーだけが失敗し、他のサーバーの結果は表示される | |
| IT-G-05 | MCP でツールを実行 | `code-chat "カレントディレクトリのファイル一覧を教えて" --mcp` | `[MCP Tool Executing]` と `[MCP Tool Result]` が表示され、ツールの結果に基づく回答が返る | |
| IT-G-06 | 対話モードの `/mcp` | 対話モードで `/mcp <プロンプト>` | MCP のツールを使って回答する。`/mcp` のみの場合は、プロンプトの指定を促すエラーが出て、対話が続く | |
| IT-G-07 | `--rag` と `--mcp` の併用 | `code-chat "認証の実装箇所を調べて" --rag --mcp` | 検索したコンテキストが、プロンプトに付加されて、MCP に渡される | |
| IT-G-08 | `--oauth --mcp` | `code-chat --oauth "ファイル一覧を教えて" --mcp` | Gemini への問い合わせは OAuth で行われ、ツールも実行される | |
| IT-G-09 | 設定ファイルがない | `mcp.json` を退避して `code-chat mcp status` | 「MCP 設定ファイルが見つかりません」の警告が表示される | |
| IT-G-10 | MCP の一時的なエラー | 無料枠で、`--mcp` を続けて実行し、429 / 503 を発生させる | リトライの警告が出て、成功する。失敗した場合は、概要とヒントが 1 回だけ表示される | |

### H. Context Caching

**有料枠の API キーが必要です (料金が発生します)。** 実施後は、`code-chat cache rm` で、作成したキャッシュを削除します。キャッシュ対象は、1024 トークン以上の内容にしてください (例: `./packages/code-chat-cli/src`)。

| ID | 確認項目 | 手順 | 期待結果 | 結果 |
| --- | --- | --- | --- | --- |
| IT-H-01 | 無料枠での作成 | 無料枠のキーで `code-chat cache create ./packages/code-chat-cli/src` | 「無料枠では Context Caching を利用できません」と、有料枠のキーを使うヒントが、1 回だけ表示される。終了コード 1 | |
| IT-H-02 | キャッシュの作成 | 有料枠のキーで `code-chat cache create ./packages/code-chat-cli/src --ttl 600` | 「キャッシュを作成しました」と、ID・対象・モデル・トークン数・有効期限が表示される | |
| IT-H-03 | 一覧 | `code-chat cache list` | H-02 のキャッシュが表示される。他のアプリケーションで作ったキャッシュは、表示されない | |
| IT-H-04 | 最新のキャッシュを使う (`-c`) | `code-chat -c "このコードの構成を説明して"` | 「キャッシュを使用します: cachedContents/...」のログが出て、回答が返る。`キャッシュヒット: N / M トークン` が出る | |
| IT-H-05 | ID を指定する (`--cache=<ID>`) | `code-chat --cache=cachedContents/<ID> "..."` | 指定したキャッシュで回答する | |
| IT-H-06 | モデルの不一致 | `code-chat -c -m <別のモデル> "..."` | 「キャッシュのモデルで実行します」のログが出て、キャッシュのモデルで回答する | |
| IT-H-07 | 指定なしでは使われない | H-02 のあとに、`-c` なしで同じ質問を実行 | 明示的なキャッシュは使われない (「キャッシュを使用します」のログが出ない) | |
| IT-H-08 | 更新 | 対象のファイルを変更して `code-chat cache update ./packages/code-chat-cli/src` | 古いキャッシュが削除されて、新しいキャッシュが作成される | |
| IT-H-09 | 小さすぎる対象 | `code-chat cache create /tmp/it/sample.py` | 「キャッシュ対象のコンテキストが小さすぎます (最小 1024 トークン)」と表示される。終了コード 1 | |
| IT-H-10 | 存在しない ID | `code-chat --cache=cachedContents/none "hi"` | 「キャッシュ '...' を取得できませんでした」と表示される。終了コード 1 | |
| IT-H-11 | キャッシュがない状態の `-c` | すべて削除したあとに `code-chat -c "hi"` | 「使用できるキャッシュがありません」と表示される。終了コード 1 | |
| IT-H-12 | 削除 | `code-chat cache rm <ID>`、および、`code-chat cache rm` (全件) | 指定した (または、全ての) キャッシュが削除される。0 件なら「削除対象のキャッシュはありません」 | |
| IT-H-13 | TTL の失効 | `--ttl 60` で作成し、90 秒後に `-c` | 失効したキャッシュは使えず、H-11 と同様のエラーになる | |
| IT-H-14 | `-c` と `--mcp` | `code-chat -c "ファイル一覧を教えて" --mcp` | 引数のエラーにはならず、Gemini API が 400 を返す想定 (`CachedContent can not be used with GenerateContent request setting system_instruction, tools or tool_config`)。**実機の結果と、実際の文言を、「5. 実施記録」に記録する** | |
| IT-H-15 | 暗黙のキャッシュ | 同じ大きなコンテキスト (`-f` で 4096 トークン以上) を先頭にして、続けて 2 回実行 | 2 回目の応答後に、`キャッシュヒット: N / M トークン` が出ることがある (発生は保証されない) | |

### I. エラーとリトライ

| ID | 確認項目 | 手順 | 期待結果 | 結果 |
| --- | --- | --- | --- | --- |
| IT-I-01 | 429 のリトライ (分あたり) | 無料枠のキーで、`seq 1 20 \| xargs -P 20 -I{} sh -c 'code-chat "{}+1=?" < /dev/null'` | 「Gemini API で一時的なエラーが発生しました (1/5): [HTTP 429 RESOURCE_EXHAUSTED] 1 分あたりのリクエスト上限に到達しました ...」の警告が出て、待ってから再試行される。リトライで成功するものがある (全件の成功は、保証しない) | |
| IT-I-02 | `retryDelay` に従う | I-01 の警告の待ち時間を確認 | 待ち時間が、エラーの `retryDelay` に、1 秒を加えた値になる | |
| IT-I-03 | 最大回数を超える | 429 が続く状態 (I-01 を、さらに大きな並列数で) | 5 回のリトライのあと、概要とヒントが 1 回だけ表示される。終了コード 1 | |
| IT-I-04 | 存在しないモデル | `code-chat -m no-such-model "hi"` | `[HTTP 404 NOT_FOUND] 指定したモデルまたはリソースが見つかりません`、`--list-models` を確認するヒント。リトライされない。終了コード 1 | |
| IT-I-05 | エラーの詳細 (`-D`) | I-04 に `-D` を付ける | 概要とヒントに加えて、`エラーの詳細:` としてレスポンスの詳細が出る。トレースバックは出ない | |
| IT-I-06 | トレースバック (`--trace`) | I-04 に `--trace` を付ける | トレースバックが出力される | |
| IT-I-07 | `--review` の失敗 | I-04 の状態で `code-chat --review -m no-such-model -f /tmp/it/sample.py` | エラーが 1 回だけ表示される。終了コード 1 | |
| IT-I-08 | ストリームの途中のエラー | 応答の途中で、ネットワークを切断する (例: 実行中に Wi-Fi を切る) | 「一部出力済みのため、リトライせずに中断します」が記録され、エラーで終了する。出力は、重複しない | |
| IT-I-09 | ネットワークが使えない | ネットワークを切断して `code-chat "hi"` | 接続エラーが、リトライされる。最終的に、エラーで終了する (終了コード 1) | |
| IT-I-10 | 1 日あたりの上限 | 1 日あたりの上限に達した状態で実行 (再現が難しいため、発生時のみ確認) | リトライされず、`1 日あたりのリクエスト上限に到達しました` と、翌日まで回復しない旨のヒントが表示される | |
| IT-I-11 | 503 のリトライ | 混雑時に実行 (再現が難しいため、発生時のみ確認) | 「モデルが高負荷で、一時的に利用できません」の警告が出て、リトライされる | |
| IT-I-12 | RAG のリトライ | 無料枠で、`--rag` の質問を続けて実行 | 検索 (埋め込み) と回答生成の 429 も、同じ形式の警告でリトライされる | |

### J. 引数の組み合わせ

引数の検証のエラーは、API を呼ぶ前に、終了コード 2 で終了します。

| ID | 確認項目 | 手順 | 期待結果 | 結果 |
| --- | --- | --- | --- | --- |
| IT-J-01 | `-f` と `--rag` | `code-chat "hi" -f /tmp/it/sample.py --rag` | 「-f/--file は --rag / --mcp と併用できません」。終了コード 2 | |
| IT-J-02 | `-f` と `--mcp` | `code-chat "hi" -f /tmp/it/sample.py --mcp` | J-01 と同様 | |
| IT-J-03 | `-c` と `-w` | `code-chat -c -w "hi"` | 「-c/--cache は -w/--write と併用できません」。終了コード 2 | |
| IT-J-04 | `-c` と `--oauth` | `code-chat -c --oauth "hi"` | 「Context Caching ... は API キーのみ対応のため、--oauth と併用できません」。終了コード 2 | |
| IT-J-05 | `cache` と `--oauth` | `code-chat cache list --oauth` | J-04 と同様 | |
| IT-J-06 | `--rag` と `--mcp` | `code-chat "hi" --rag --mcp --dry-run` | エラーにならない (併用できる) | |
| IT-J-07 | 位置引数のプロンプト | `code-chat "rag とは何ですか" --dry-run` | 最初の位置引数が `rag` / `cache` / `mcp` と一致するときだけ、サブコマンドとして扱われる。ここでは、全体がプロンプトとして解析され、サブコマンドは `None` になる | |
| IT-J-08 | 不明なオプション | `code-chat --no-such-option` | エラーで終了する (`unrecognized arguments`) | |

## 5. 実施記録

実施のたびに、次の表へ、追記してください。NG や、想定と異なる結果があった項目は、詳細を書いてください。

| 実施日 | 実施者 | バージョン (コミット) | 使用した認証 | 結果の概要 | NG の項目と詳細 |
| --- | --- | --- | --- | --- | --- |
| | | | | | |

### 参考: これまでの確認 (開発時のスポット確認)

正式な実施記録ではなく、開発中に、個別に確認した内容です。

- 無料枠の API キーで、埋め込み API に 160 件を同時に送り、429 が多数出たが、リトライで 160 件すべて成功した (2026-09-26、RAG のリトライ)。
- `--oauth` と無料枠の API キーで、`gemini-3.5-flash` の 429 が、同じ上限 (1 分あたり 5 リクエスト、`GenerateRequestsPerMinutePerProjectPerModel-FreeTier`) を示した (2026-09-26、A-08 に相当)。
- 有料枠の API キーで、キャッシュの作成・一覧・`-c`・`--cache=<ID>`・更新・削除を確認した (キャッシュヒットは、100%。H-02〜H-08、H-12 に相当)。
- `-c` と `--mcp` を併用した場合の Gemini API のエラー (H-14) は、**未確認**です。
